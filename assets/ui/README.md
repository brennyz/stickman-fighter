# assets/ui — overige UI-SVG

Gedeelde UI-iconen als bestanden (ASSET-STYLE):

| Map / file | Gebruik |
|------------|---------|
| `saga/*.svg` | Character/skill saga-filter chips |
| `islands/*.svg` | Adventure eiland-tabs & help |
| `ach/*.svg` | Prestatie-lijst iconen |
| `lock.svg` / `check.svg` | Locked levels, claimed |
| `star.svg` / `star-empty.svg` | Level-sterren |
| `pause.svg` / `dice-d20.svg` / `coin.svg` | Pause-knop, help d20, shop |

Zie **`ASSET-STYLE.md`** (repo-root) voor tokens en workflow.
