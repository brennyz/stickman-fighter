Winter/Christmas. Same slot names as jungle/halloween. See `SEASON-OVERLAY.md`.

CSS fallbacks (frost, icicles, snow wash) are live. Drop PNGs here, then uncomment `--season-art-*` in `styles/seasons.css`.
